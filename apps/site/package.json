{
  "name": "@crewhelm/site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro build && node ../../scripts/validate-site-output.mjs dist",
    "deploy:production": "pnpm run build && wrangler deploy",
    "dev": "astro dev",
    "preview": "astro preview",
    "preview:upload": "pnpm run build && wrangler versions upload"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "@astrojs/starlight": "0.41.6",
    "@crewhelm/design": "workspace:*",
    "astro": "7.1.6"
  },
  "devDependencies": {
    "@tailwindcss/vite": "4.3.3",
    "tailwindcss": "4.3.3",
    "wrangler": "4.115.0"
  }
}
